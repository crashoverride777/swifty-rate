import XCTest
@testable import swifty_rate

final class swifty_rateTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swifty_rate().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
